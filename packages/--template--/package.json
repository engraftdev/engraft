{
  "name": "@engraft/--template--",
  "version": "0.0.1",
  "description": "DESCRIPTION TODO",
  "scripts": {
    "test:watch": "vitest",
    "test": "vitest run",
    "tsc": "tsc",
    "lint": "eslint --ext .ts,.tsx src"
  },
  "main": "src/index.js",
  "dependencies": {
    "@engraft/shared": "^0.0.1"
  }
}
